% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{mat_lasso}
\alias{mat_lasso}
\title{Solve the matrix, wrapper.}
\usage{
mat_lasso(
  G,
  g,
  lambda,
  alpha = 1,
  weak = F,
  Grp = NULL,
  C_init = NULL,
  pb = NULL
)
}
\arguments{
\item{G}{The Gram matrix X'X / n}

\item{g}{The matrix X'y / n}

\item{lambda}{The regularization parameter}

\item{alpha}{The update weight}

\item{weak}{True for weak Lasso, False for standard Lasso}

\item{Grp}{The group assignment matrix, indexes starting from 1}

\item{C_init}{The initial value of C}

\item{pb}{A progress bar function}
}
\value{
C The estimated coefficients matrix
}
\description{
Solve the matrix, wrapper.
}
\examples{
# Generate random data
set.seed(123)
n <- 100
p <- 20
X <- matrix(rnorm(n * p), n, p)
Y <- matrix(rnorm(n * p), n, p)
# Compute Gram matrix and cross-product
G <- crossprod(X) / n
g <- crossprod(X, Y) / n
# Set regularization parameter
lambda <- 0.1
# Estimate matrix with Lasso penalty
C_est <- mat_lasso(G, g, lambda, alpha = 1, weak = FALSE)
}
